# Project-scoped Codex defaults. Keep provider/auth in user-level ~/.codex/config.toml.

approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "cached"
personality = "pragmatic"
project_doc_max_bytes = 32768

[features]
multi_agent = true
hooks = true

